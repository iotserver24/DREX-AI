/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: '.next',
  images: {
    unoptimized: true,
  },
  transpilePackages: ['drex-core'],
}

module.exports = nextConfig
